// Promoted to the shared redesign primitives when the Meetings list needed
// the same pager — kept as a re-export so Tasks' imports stay put.
export { default, PAGE_SIZE_OPTIONS } from "@/Components/Redesign/primitives/Pagination";
