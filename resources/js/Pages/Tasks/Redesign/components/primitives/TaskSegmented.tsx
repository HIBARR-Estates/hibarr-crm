// Promoted to the shared redesign primitives when the Meetings page needed
// the same control — kept as a re-export so Tasks' imports stay put.
export { default } from "@/Components/Redesign/primitives/Segmented";
export type { SegmentedOption } from "@/Components/Redesign/primitives/Segmented";
