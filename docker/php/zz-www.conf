; Loaded after the image's zz-docker.conf so these pool values win.
[www]
clear_env = no

pm = dynamic
pm.max_children = 20
pm.start_servers = 5
pm.min_spare_servers = 3
pm.max_spare_servers = 10
pm.max_requests = 500
